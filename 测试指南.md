# 测试指南

## ✅ 模块加载测试（已完成）

测试结果显示：
- ✅ 使用绝对路径可以成功加载 auth-helper
- ✅ 模块导出正确（包含 authHelper）

## 🚀 应用测试步骤

### 1. 检查应用是否启动

应用应该已经在运行。如果没有，执行：
```bash
npm start
```

### 2. 打开开发者工具

在 Electron 窗口中：
- **Mac**: 按 `Cmd + Option + I`
- **Windows**: 按 `F12` 或 `Ctrl + Shift + I`

### 3. 查看控制台输出

在 Console 标签页中，你应该看到：

#### 如果加载成功：
```
📦 加载 auth-helper...
   当前目录 (__dirname): /Users/yugongtian/NPDF/src/scripts
   目标路径: /Users/yugongtian/NPDF/src/utils/auth-helper.js
✅ auth-helper 加载成功！
```

#### 如果加载失败：
```
❌ 加载 auth-helper 失败: ...
⚠️ 使用降级模式：认证功能暂时不可用，但应用可以正常运行
```

### 4. 测试应用功能

#### 场景A：auth-helper 加载成功

1. **检查登录状态**
   - 如果未登录，应该自动跳转到 `auth.html` 登录页面
   - 如果已登录，应该显示主界面

2. **测试注册功能**
   - 在登录页面点击"注册"
   - 填写邮箱和密码
   - 点击"注册"按钮
   - 应该显示"注册成功！请登录"

3. **测试登录功能**
   - 填写邮箱和密码
   - 点击"登录"按钮
   - 应该跳转到主界面

4. **检查用户信息**
   - 侧边栏底部应该显示用户信息
   - 显示用户名和邮箱

5. **测试登出功能**
   - 点击退出按钮
   - 确认后应该跳转到登录页面

#### 场景B：auth-helper 加载失败（降级模式）

1. **应用应该能正常启动**
   - 主界面可以正常显示
   - 不会崩溃

2. **基本功能应该正常**
   - 可以打开文件
   - 可以阅读文档
   - 可以使用标注功能

3. **认证功能不可用**
   - 不会跳转到登录页面
   - 侧边栏不显示用户信息
   - 这是正常的，因为模块未加载

## 📊 测试结果记录

### 测试日期：___________

### 模块加载状态：
- [ ] ✅ auth-helper 加载成功
- [ ] ❌ auth-helper 加载失败（降级模式）

### 功能测试结果：

#### 如果加载成功：
- [ ] 登录页面正常显示
- [ ] 注册功能正常
- [ ] 登录功能正常
- [ ] 用户信息正常显示
- [ ] 登出功能正常

#### 如果加载失败：
- [ ] 应用可以正常启动
- [ ] 主界面正常显示
- [ ] 文件打开功能正常
- [ ] 阅读功能正常

### 发现的问题：
1. ________________________________
2. ________________________________
3. ________________________________

## 🔧 如果遇到问题

### 问题1：应用无法启动
- 检查终端是否有错误信息
- 检查 Node.js 版本是否兼容
- 检查依赖是否安装完整

### 问题2：auth-helper 加载失败
- 查看控制台的错误信息
- 检查文件路径是否正确
- 告诉我具体的错误信息，我会帮你修复

### 问题3：功能不正常
- 查看控制台的错误信息
- 检查网络连接（Supabase 需要网络）
- 检查 Supabase 配置是否正确

## 💡 提示

- 即使认证功能暂时不可用，应用的其他功能都应该正常工作
- 如果看到警告信息，应用仍然可以正常使用
- 所有错误信息都会在控制台显示，方便调试

