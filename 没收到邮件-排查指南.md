# 没收到验证邮件 - 排查指南

## 🔍 快速排查步骤

### 步骤 1：检查邮箱

1. **检查收件箱**
   - 打开你的邮箱（Gmail、Outlook、QQ邮箱等）
   - 查看收件箱（Inbox）
   - 邮件主题通常是 "Confirm your signup" 或类似内容

2. **检查垃圾邮件文件夹**
   - 很多邮件服务会把验证邮件标记为垃圾邮件
   - 检查"垃圾邮件"、"Spam"、"已删除邮件"等文件夹
   - 如果找到了，标记为"不是垃圾邮件"

3. **等待几分钟**
   - 邮件可能需要 1-5 分钟才能送达
   - 不要立即认为邮件发送失败

4. **检查邮箱地址**
   - 确认注册时输入的邮箱地址是否正确
   - 检查是否有拼写错误

### 步骤 2：使用重新发送功能

如果没收到邮件，可以使用应用中的"重新发送验证邮件"功能：

1. **在登录页面**
   - 如果你刚注册，应该能看到"没收到验证邮件？重新发送"链接
   - 点击这个链接

2. **等待新邮件**
   - 系统会重新发送验证邮件
   - 再次检查邮箱（包括垃圾邮件文件夹）

### 步骤 3：检查 Supabase 配置

如果重新发送后仍然没收到，可能是 Supabase 配置问题：

#### 3.1 检查邮件服务状态

1. 登录 Supabase Dashboard：https://app.supabase.com
2. 进入你的项目
3. 进入 **Authentication** → **Settings**
4. 查看 **Email Auth** 部分
5. 确认邮件服务是否正常

#### 3.2 检查邮件日志

1. 在 Supabase Dashboard 中
2. 进入 **Logs** → **Auth Logs**
3. 查看是否有邮件发送记录
4. 检查是否有错误信息

#### 3.3 检查重定向 URL 配置

1. 进入 **Authentication** → **URL Configuration**
2. 检查 **Redirect URLs** 是否配置正确
3. 确保包含你的应用 URL

### 步骤 4：检查 Supabase 邮件限制

Supabase 免费版可能有邮件发送限制：

1. **每日邮件限制**
   - 免费版可能限制每日邮件数量
   - 检查是否达到限制

2. **邮件服务状态**
   - 检查 Supabase 服务状态页面
   - 确认邮件服务是否正常运行

## 🛠️ 解决方案

### 方案 1：使用重新发送功能（推荐）

1. 在登录页面点击"没收到验证邮件？重新发送"
2. 等待新邮件
3. 检查邮箱（包括垃圾邮件）

### 方案 2：检查 Supabase 邮件配置

如果重新发送多次仍然没收到，可能是配置问题：

#### 2.1 检查邮件模板

1. 进入 **Authentication** → **Email Templates**
2. 选择 **"Confirm signup"** 模板
3. 确认模板配置正确

#### 2.2 配置自定义 SMTP（如果需要）

如果 Supabase 默认邮件服务有问题，可以配置自己的 SMTP：

1. 进入 **Settings** → **Auth**
2. 找到 **SMTP Settings**
3. 配置你的邮件服务器（如 Gmail、SendGrid 等）

### 方案 3：临时禁用邮箱验证（仅开发环境）

如果是开发环境，可以临时禁用邮箱验证：

1. 进入 **Authentication** → **Settings**
2. 取消勾选 **"Enable email confirmations"**
3. 这样注册后可以立即登录，不需要验证邮箱

⚠️ **注意**：生产环境不建议禁用邮箱验证

### 方案 4：手动验证账号（仅管理员）

如果你是项目管理员，可以在 Supabase Dashboard 中手动验证用户：

1. 进入 **Authentication** → **Users**
2. 找到未验证的用户
3. 点击用户，查看详情
4. 手动设置 `email_confirmed_at` 字段

## 📧 邮件发送方信息

Supabase 验证邮件通常来自：
- **发送方**：`noreply@mail.app.supabase.com` 或类似地址
- **主题**：`Confirm your signup` 或 `确认你的注册`
- **内容**：包含验证链接

## 🔍 详细排查清单

- [ ] 检查收件箱
- [ ] 检查垃圾邮件文件夹
- [ ] 等待 5 分钟
- [ ] 确认邮箱地址正确
- [ ] 使用重新发送功能
- [ ] 检查 Supabase 邮件日志
- [ ] 检查 Supabase 邮件服务状态
- [ ] 检查重定向 URL 配置
- [ ] 检查邮件模板配置
- [ ] 检查是否达到邮件发送限制

## 💡 常见原因

1. **邮件被标记为垃圾邮件**（最常见）
   - 解决方案：检查垃圾邮件文件夹，标记为"不是垃圾邮件"

2. **邮件延迟送达**
   - 解决方案：等待几分钟

3. **邮箱地址错误**
   - 解决方案：确认注册时输入的邮箱地址

4. **Supabase 邮件服务问题**
   - 解决方案：检查 Supabase 服务状态，或配置自定义 SMTP

5. **达到邮件发送限制**
   - 解决方案：等待一段时间后重试，或升级 Supabase 计划

## 🆘 如果所有方法都失败

如果尝试了所有方法仍然没收到邮件：

1. **联系 Supabase 支持**
   - 访问 Supabase 支持页面
   - 提供你的项目信息和问题描述

2. **使用替代邮箱**
   - 尝试使用不同的邮箱地址注册
   - 某些邮箱服务可能对验证邮件更友好

3. **检查网络和防火墙**
   - 确认网络连接正常
   - 检查防火墙是否阻止了邮件

4. **查看浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 标签，检查是否有错误信息

## 📝 测试邮件发送

如果你想测试 Supabase 邮件服务是否正常：

1. 在 Supabase Dashboard 中
2. 进入 **Authentication** → **Users**
3. 找到测试用户
4. 点击用户，选择 "Resend confirmation email"
5. 检查是否收到邮件

---

**提示**：大多数情况下，邮件都在垃圾邮件文件夹中。请务必检查！

