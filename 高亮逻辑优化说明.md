# 高亮逻辑优化说明

## 问题描述

之前的高亮逻辑存在以下问题：
1. **左键选择文本时**：浏览器显示默认的蓝色 `::selection` 高亮
2. **同时**：JavaScript 代码会调用 `showSelectionHighlight()` 添加额外的 `.selection-preview` 类，创建第二层高亮
3. **右键点击后**：又创建新的 `.word-highlighted` 类，形成第三层高亮

这导致了多层高亮叠加、视觉效果混乱、可能出现位置错位等问题。

## 正确的逻辑

**核心思想**：将浏览器默认的蓝色选择高亮（`::selection`）平滑转换为我们的自定义高亮样式，而不是创建多层高亮。

### 工作流程

1. **左键选择文本时**：
   - 只显示浏览器原生的 `::selection` 样式（蓝色半透明高亮）
   - 不添加任何额外的预览高亮层
   - 保持简洁和性能

2. **右键点击选择高亮颜色后**：
   - 获取当前选中的 span 元素
   - 为这些 span 添加 `.word-highlighted` 类和对应的颜色类
   - **立即清除浏览器的文本选择状态** (`selection.removeAllRanges()`)
   - 这样蓝色的 `::selection` 高亮消失，只保留我们的自定义高亮

### 视觉效果

用户体验：
- 左键拖动选择文本 → 看到蓝色半透明高亮（原生 `::selection`）
- 右键选择颜色（如黄色） → 蓝色高亮瞬间变成黄色高亮
- 效果自然流畅，没有多层叠加

## 代码修改

### 1. JavaScript 修改 (`src/scripts/reader.js`)

#### 修改点 1：移除预览高亮功能
```javascript
// 之前（第945-947行）
this.showHighlightTools();
// 立即显示连贯的高亮预览
this.showSelectionHighlight();

// 之后
this.showHighlightTools();
// 🎯 不再添加额外的预览高亮层，直接使用浏览器的 ::selection 样式
// 这样用户看到的就是默认的蓝色选择高亮，右键点击后会转换为自定义高亮
```

#### 修改点 2：优化 `highlightSelectedText()` 方法
```javascript
// 移除了调用 hideSelectionHighlight() 的代码
// 添加了立即清除选择状态的逻辑
selection.removeAllRanges();
this.currentSelection = null;
```

#### 修改点 3：优化 `highlightSelectedTextWithColor()` 方法
```javascript
// 移除了调用 hideSelectionHighlight() 的代码
// 添加了清除选择状态的逻辑
const selection = this.currentSelection || window.getSelection();
if (selection && selection.rangeCount > 0) {
    selection.removeAllRanges();
}
this.currentSelection = null;
this.selectedSpans = null;
```

### 2. CSS 修改 (`src/styles/reader.css`)

#### 修改点 1：优化 `::selection` 样式
```css
/* 之前 */
.pdf-text-layer ::selection {
    background: rgba(74, 144, 226, 0.3);
    color: transparent;
}

/* 之后 */
.pdf-text-layer ::selection {
    background: rgba(74, 144, 226, 0.35); /* 稍微提高不透明度 */
    color: inherit; /* 保持文本颜色可见 */
}
```

#### 修改点 2：简化已高亮文本的选择样式
```css
/* 之前 */
.pdf-text-layer span.word-highlighted::selection {
    background: transparent !important;
    color: transparent !important;
}

/* 之后 */
.pdf-text-layer span.word-highlighted::selection {
    background: transparent !important;
}
```

#### 修改点 3：标记弃用的预览样式
```css
/* 选择预览高亮样式（已弃用 - 现在直接使用浏览器的 ::selection 样式） */
/* 保留这些样式以备将来使用 */
.pdf-text-layer span.selection-preview { ... }
```

## 技术优势

1. **性能更好**：减少了 DOM 操作和额外的高亮层
2. **更准确**：直接利用浏览器原生的文本选择机制
3. **视觉更自然**：从蓝色选择到自定义高亮的过渡更流畅
4. **代码更简洁**：移除了不必要的预览高亮逻辑
5. **避免错位**：不会出现多层高亮导致的位置错位问题

## 测试建议

1. 打开 PDF 文档
2. 左键拖动选择一段文本 → 应该看到蓝色半透明高亮
3. 右键点击，选择一个高亮颜色（如黄色）
4. 观察：蓝色高亮应该立即变成黄色高亮，没有闪烁或多层叠加
5. 再次选择已高亮的文本 → 不应该显示蓝色选择高亮

## 日期

2025年11月2日

