# 荧光笔效果 - 最终版

## 🎯 最终效果

真实的荧光笔高亮效果：
1. ✅ 上部空间少，下部空间多（符合书写习惯）
2. ✅ 左右有适当空间
3. ✅ 四个角是圆弧状（柔和美观）
4. ✅ 背景在文字下方

## 📐 参数设置

```css
.pdf-text-layer span {
    /* 不对称 padding：上少下多 */
    padding: 1px 2px 4px 2px;
    /*       ↑   ↑   ↑   ↑
             上  右  下  左
             1px 2px 4px 2px  */
    
    /* 负 margin 抵消，保持文字位置 */
    margin: -1px -2px -4px -2px;
    
    /* 圆角 */
    border-radius: 3px;
    
    /* 行高 */
    line-height: 1.3;
}
```

## 🎨 视觉效果

```
侧视图（显示上下空间）：

上部（1px）
─────────────
  setting
─────────────
─────────────  下部（4px）
─────────────
```

```
正视图（圆角效果）：

╭──────────────╮
│              │
│  setting     │  ← 圆角荧光笔效果
│              │
╰──────────────╯
```

## 🔧 参数说明

### padding 参数

```css
padding: 1px 2px 4px 2px;
```

- **上 (1px)**: 顶部留少量空间
- **右 (2px)**: 右侧留适当空间
- **下 (4px)**: 底部留较多空间（重点！）
- **左 (2px)**: 左侧留适当空间

### border-radius 参数

```css
border-radius: 3px;
```

- `3px` = 圆角半径
- 值越大，圆角越明显
- 建议范围：`2px - 5px`

### 为什么下部空间更多？

真实的荧光笔使用习惯：
```
书写时：
┌────────┐
│ text   │  ← 笔尖在上方，空间少
│        │
└────────┘  ← 笔尾在下方，空间多
```

## 📊 效果对比

### 对称 padding（之前）

```css
padding: 2px 1px 2px 1px;  /* 上下相同 */
```

```
┌──────┐
│      │  ← 2px
│ text │
│      │  ← 2px
└──────┘
```

### 不对称 padding（现在）

```css
padding: 1px 2px 4px 2px;  /* 上少下多 */
```

```
┌──────┐
│      │  ← 1px (少)
│ text │
│      │
│      │  ← 4px (多)
└──────┘
```

更符合真实荧光笔效果！✅

## 🎨 圆角效果

### 无圆角

```
┌──────────┐
│  text    │
└──────────┘
```

### 有圆角 (3px)

```
╭──────────╮
│  text    │
╰──────────╯
```

更柔和、更美观！✅

## 🔄 如何调整

### 想要更高的高亮

```css
padding: 2px 2px 5px 2px;  /* 增加上下 */
margin: -2px -2px -5px -2px;
```

### 想要更宽的高亮

```css
padding: 1px 3px 4px 3px;  /* 增加左右 */
margin: -1px -3px -4px -3px;
```

### 想要更圆的角

```css
border-radius: 5px;  /* 增加圆角半径 */
```

### 想要更方的角

```css
border-radius: 1px;  /* 减小圆角半径 */
```

## 💡 技术细节

### 为什么用负 margin？

```
1. 添加 padding
   → 背景扩展了
   → 但文字位置也移动了 ❌

2. 添加负 margin
   → 抵消 padding 的位移
   → 背景扩展，文字位置不变 ✅
```

### border-radius 如何工作？

```css
border-radius: 3px;
```

浏览器会：
1. 在四个角绘制半径为 3px 的圆弧
2. 背景色沿着圆弧边缘渲染
3. 形成圆角效果

### backgroundColor 的渲染范围

```
渲染范围 = content + padding
圆角范围 = border-radius
```

所以：
- `padding` 控制背景大小
- `border-radius` 控制角的形状
- `margin` 控制元素位置

## 🧪 测试验证

### 测试步骤

1. 打开 PDF 文档
2. 选择文字 "setting the action"
3. 右键选择黄色高亮
4. 观察效果：
   - ✅ 上部空间较少
   - ✅ 下部空间较多
   - ✅ 四个角是圆弧
   - ✅ 整体像荧光笔效果

### 预期效果

```
╭═══════════════════════╮
│                       │
│  setting the action   │  ← 黄色荧光笔
│                       │
│                       │
╰═══════════════════════╯
```

## 📝 完整代码

### CSS

```css
.pdf-text-layer span {
    color: transparent;
    position: absolute;
    white-space: pre;
    cursor: text;
    transform-origin: 0% 0%;
    user-select: text;
    z-index: 2;
    
    /* 荧光笔效果 */
    padding: 1px 2px 4px 2px;      /* 上少下多 */
    margin: -1px -2px -4px -2px;   /* 抵消位移 */
    border-radius: 3px;            /* 圆角 */
    line-height: 1.3;              /* 行高 */
    box-sizing: border-box;
}
```

### JavaScript

```javascript
// 直接设置背景色
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';

// CSS 自动处理：
// - padding 扩展背景
// - margin 保持位置
// - border-radius 添加圆角
```

## 🎯 总结

### 关键参数

```css
padding: 1px 2px 4px 2px;      /* 不对称，下部更多 */
margin: -1px -2px -4px -2px;   /* 抵消位移 */
border-radius: 3px;            /* 圆角 */
```

### 效果

✅ 真实的荧光笔效果  
✅ 上少下多的空间分布  
✅ 柔和的圆角  
✅ 文字位置准确  

---

**日期**：2025年11月2日  
**版本**：5.0（最终版 - 不对称圆角荧光笔）

