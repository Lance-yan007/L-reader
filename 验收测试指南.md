# 🎯 文本位置识别模块验收测试指南

## 📋 测试准备

### 1. 应用启动状态检查
- ✅ Electron应用已启动（进程ID: 44831, 44834等）
- ✅ 主窗口应该已经打开
- ✅ 应该能看到主界面

### 2. 测试文件准备
请准备以下测试文件：
- 📄 一个PDF文件（建议包含中英文混合文本）
- 📄 一个文本文件（.txt）
- 📄 一个Word文档（.docx，可选）

## 🧪 功能验收测试

### 第一阶段：基础功能测试

#### 1. 文件加载测试
```
操作步骤：
1. 点击"选择文件"按钮
2. 选择一个PDF文件
3. 观察控制台输出

预期结果：
- 文件成功加载
- 控制台显示"PDF文档解析成功，页数: X"
- 控制台显示"文本位置检测器初始化成功"
- 控制台显示"页面 X 提取了 Y 个字符"
```

#### 2. 字符信息提取测试
```
操作步骤：
1. 打开浏览器开发者工具（F12）
2. 在控制台输入以下代码：

// 获取字符信息存储
const storage = readerApp.textPositionDetector.getCharacterStorage();
console.log('字符存储:', storage);

// 获取第1页的字符信息
const page1Chars = readerApp.textPositionDetector.getPageCharacters(1);
console.log('第1页字符数量:', page1Chars.length);
console.log('前10个字符:', page1Chars.slice(0, 10));

预期结果：
- 显示字符存储对象
- 显示第1页的字符数量（应该 > 0）
- 显示前10个字符的详细信息
```

### 第二阶段：位置检测测试

#### 3. 字符位置查找测试
```
操作步骤：
1. 在控制台输入：

// 在指定位置查找字符
const char = readerApp.textPositionDetector.findCharacterAtPosition(100, 200, 1);
console.log('位置(100,200)的字符:', char);

// 在矩形区域内查找字符
const rectChars = readerApp.textPositionDetector.findCharactersInRect({
    x: 50, y: 50, width: 200, height: 100
}, 1);
console.log('矩形区域内的字符:', rectChars);

预期结果：
- 能找到指定位置的字符
- 能返回矩形区域内的字符列表
```

#### 4. 文本查找测试
```
操作步骤：
1. 在控制台输入：

// 查找特定文本
const textResults = readerApp.textPositionDetector.findTextPositions('关键词');
console.log('找到的文本位置:', textResults);

// 获取页面文本内容
const pageText = readerApp.textPositionDetector.getPageText(1);
console.log('第1页文本内容:', pageText.substring(0, 100) + '...');

预期结果：
- 能找到指定文本的位置
- 能获取完整的页面文本内容
```

### 第三阶段：高级功能测试

#### 5. 字符关系分析测试
```
操作步骤：
1. 在控制台输入：

// 获取第一个字符
const firstChar = readerApp.textPositionDetector.getPageCharacters(1)[0];
console.log('第一个字符:', firstChar);

// 获取相邻字符
const adjacent = readerApp.textPositionDetector.getAdjacentCharacters(firstChar, 20);
console.log('相邻字符:', adjacent);

// 获取同行字符
const lineChars = readerApp.textPositionDetector.getLineCharacters(firstChar);
console.log('同行字符:', lineChars);

预期结果：
- 能获取字符的相邻字符
- 能获取字符的同行字符
```

#### 6. 统计信息测试
```
操作步骤：
1. 在控制台输入：

// 获取字符统计信息
const stats = readerApp.textPositionDetector.getCharacterStatistics();
console.log('字符统计:', stats);

// 获取存储信息
const storageInfo = readerApp.textPositionDetector.getStorageInfo();
console.log('存储信息:', storageInfo);

预期结果：
- 显示总页数、总字符数等统计信息
- 显示存储大小、内存使用等信息
```

### 第四阶段：坐标转换测试

#### 7. 坐标转换测试
```
操作步骤：
1. 在控制台输入：

// 获取坐标转换器
const converter = readerApp.textPositionDetector.coordinateConverter;
console.log('坐标转换器状态:', converter.getStatus());

// 测试坐标转换
const pdfCoords = {x: 100, y: 200};
const screenCoords = converter.pdfToScreen(pdfCoords.x, pdfCoords.y);
console.log('PDF坐标转屏幕坐标:', pdfCoords, '->', screenCoords);

const backToPdf = converter.screenToPdf(screenCoords.x, screenCoords.y);
console.log('屏幕坐标转PDF坐标:', screenCoords, '->', backToPdf);

预期结果：
- 坐标转换器状态正常
- 坐标转换结果正确
- 往返转换保持精度
```

## 🐛 常见问题排查

### 问题1：字符信息为空
```
可能原因：
- 脚本加载失败
- PDF解析失败

解决方案：
1. 检查浏览器控制台是否有错误
2. 确认所有脚本文件都已正确加载
3. 重新加载页面
```

### 问题2：坐标转换不准确
```
可能原因：
- 页面高度设置错误
- 缩放级别不正确

解决方案：
1. 检查坐标转换器的页面高度设置
2. 确认缩放级别设置正确
```

### 问题3：字符查找失败
```
可能原因：
- 坐标超出页面范围
- 容差设置过小

解决方案：
1. 增加查找容差
2. 确认坐标在页面范围内
```

## ✅ 验收标准

### 基础功能（必须通过）
- [ ] PDF文件能正常加载
- [ ] 字符信息能正确提取
- [ ] 字符位置查找功能正常
- [ ] 文本内容能正确获取

### 高级功能（建议通过）
- [ ] 字符关系分析正常
- [ ] 坐标转换准确
- [ ] 统计信息正确
- [ ] 性能表现良好

### 性能要求
- [ ] 页面加载时间 < 5秒
- [ ] 字符提取时间 < 3秒
- [ ] 内存使用合理

## 🎉 验收完成

如果以上所有测试都通过，说明文本位置识别模块已经成功实现并集成到您的阅读器中！

### 下一步建议：
1. 测试不同类型的PDF文件
2. 测试大量文本的PDF文件
3. 测试复杂布局的PDF文件
4. 根据实际使用情况优化性能
