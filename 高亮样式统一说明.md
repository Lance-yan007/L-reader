# 高亮样式统一说明

## 🎯 目标

让右键高亮的样式（黄色、绿色等）和默认蓝色 `::selection` 高亮的**大小、高度完全一致**。

## ✅ 实现方案

### 1. **删除所有复杂的 CSS 规则**

之前的实现使用了：
- `.word-highlighted` 类
- `.color-yellow`、`.color-green` 等颜色类
- `::before` 伪元素
- 复杂的定位和 margin 调整

**现在全部删除！**

### 2. **确保 span 元素样式一致**

```css
.pdf-text-layer span {
    color: transparent;
    position: absolute;
    white-space: pre;
    cursor: text;
    transform-origin: 0% 0%;
    user-select: text;
    z-index: 2;
    
    /* 🎯 关键：确保背景色完全贴合文本 */
    padding: 0;
    margin: 0;
    box-sizing: content-box;
    line-height: 1;
}
```

**关键点**：
- `padding: 0` - 没有内边距
- `margin: 0` - 没有外边距
- `box-sizing: content-box` - 背景只覆盖内容区域
- `line-height: 1` - 行高为 1，背景紧贴文本

### 3. **JavaScript 直接设置背景色**

```javascript
// 就这一行！
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';
```

**工作原理**：
- 浏览器会自动让背景色**完全贴合文本内容**
- 不会有额外的空间
- 和 `::selection` 的渲染机制**完全一致**

---

## 📊 对比

### 浏览器的 `::selection` 样式

```css
.pdf-text-layer ::selection {
    background: rgba(74, 144, 226, 0.35);
    color: inherit;
}
```

**浏览器做的事情**：
1. 检测到文本被选中
2. 应用 `background` 颜色
3. 背景**自动贴合文本**，不添加额外空间
4. 高度 = 文本高度

### 我们的高亮样式

```javascript
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';
```

**浏览器做的事情**：
1. 检测到 `backgroundColor` 属性
2. 应用背景颜色
3. 背景**自动贴合文本**，不添加额外空间
4. 高度 = 文本高度

**完全一样！** ✅

---

## 🎨 视觉效果

### 之前（使用伪元素）

```
┌─────────────────────────────┐
│  ::before 伪元素（黄色背景）  │  ← 可能有额外的高度
│  ┌───────────────────────┐  │
│  │   文本内容            │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

**问题**：
- 伪元素的高度需要手动调整（`min-height`、`margin-top` 等）
- 很难和 `::selection` 完全一致
- 容易出现高度不匹配

### 现在（使用 backgroundColor）

```
┌───────────────────────┐
│   文本内容（黄色背景） │  ← 背景完全贴合文本
└───────────────────────┘
```

**优势**：
- 背景自动贴合文本
- 高度自动匹配
- 和 `::selection` 完全一致

---

## 🔧 技术细节

### 为什么 `backgroundColor` 能自动贴合？

浏览器的渲染引擎对 `background-color` 的处理：

```
1. 计算文本的边界框（Bounding Box）
   ↓
2. 在边界框内填充背景色
   ↓
3. 背景色的范围 = 文本内容的范围
   ↓
4. 不受 padding、margin 影响（因为我们设置为 0）
```

### 为什么和 `::selection` 一致？

因为 `::selection` 伪元素本质上也是设置 `background-color`：

```css
/* 这两个的渲染机制完全相同 */

/* 方式1：::selection */
::selection {
    background: rgba(74, 144, 226, 0.35);
}

/* 方式2：直接设置 */
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';
```

---

## 📝 修改清单

### CSS 文件 (`src/styles/reader.css`)

1. ✅ 为 `.pdf-text-layer span` 添加了基础样式：
   - `padding: 0`
   - `margin: 0`
   - `box-sizing: content-box`
   - `line-height: 1`

2. ✅ 删除了所有旧的高亮 CSS 规则：
   - `.word-highlighted` 类
   - `.color-*` 颜色类
   - `::before` 伪元素样式

3. ✅ 保留了 `::selection` 样式（用于临时选择）

### JavaScript 文件 (`src/scripts/reader.js`)

1. ✅ `highlightSelectedText()` - 直接设置 `backgroundColor`
2. ✅ `highlightSelectedTextWithColor()` - 直接设置 `backgroundColor`
3. ✅ `highlightSingleWordWithColor()` - 直接设置 `backgroundColor`
4. ✅ `removeHighlightFromSpan()` - 直接清除 `backgroundColor`

---

## 🎯 测试验证

### 测试步骤

1. **打开 PDF 文档**

2. **测试默认选择**：
   - 左键拖动选择文本
   - 观察蓝色高亮的高度和范围

3. **测试右键高亮**：
   - 保持选择，右键选择黄色
   - 观察黄色高亮的高度和范围

4. **对比验证**：
   - 蓝色高亮和黄色高亮的高度应该**完全一致**
   - 背景应该都**紧贴文本**，没有额外空间

### 预期结果

```
蓝色 ::selection 高亮：
┌───────────────┐
│  Hello World  │  ← 高度 = 文本高度
└───────────────┘

黄色 backgroundColor 高亮：
┌───────────────┐
│  Hello World  │  ← 高度 = 文本高度（完全一致）
└───────────────┘
```

---

## 💡 总结

### 核心原理

> 浏览器的 `background-color` 会自动贴合元素的内容区域。
> 只要确保 `padding: 0` 和 `margin: 0`，背景色就会完全贴合文本。

### 实现方式

```javascript
// 1. 确保 span 没有额外空间（CSS）
span {
    padding: 0;
    margin: 0;
    box-sizing: content-box;
    line-height: 1;
}

// 2. 直接设置背景色（JavaScript）
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';

// 完成！浏览器自动处理一切！
```

### 为什么这样做是对的？

1. ✅ 和浏览器 `::selection` 机制完全一致
2. ✅ 不需要手动调整高度、位置
3. ✅ 代码极其简单
4. ✅ 不会出错
5. ✅ 性能最优

---

**日期**：2025年11月2日  
**版本**：3.0（样式统一版）

