# 荧光笔效果实现说明

## 🎯 目标效果

实现真实的荧光笔高亮效果：
1. **高亮是文字的背景层**（在文字下面）
2. **高亮包裹文字**（上下左右都有空间）
3. **高度覆盖整行**（像真实的荧光笔）

## ✅ 实现方案

### 核心思路

使用 `padding` 让背景扩展，用负 `margin` 保持文字位置不变：

```css
.pdf-text-layer span {
    /* 添加 padding，让背景色扩展 */
    padding: 2px 1px; /* 上下2px，左右1px */
    
    /* 用负 margin 抵消 padding，保持文字位置不变 */
    margin: -2px -1px;
    
    /* 增加行高，让背景覆盖更多空间 */
    line-height: 1.3;
    
    box-sizing: border-box;
}
```

### 工作原理

```
原始文字位置：
┌─┐
│A│
└─┘

添加 padding 后（背景扩展）：
┌─────┐
│  A  │  ← 背景变大了
└─────┘
但文字位置也移动了 ❌

添加负 margin 后（文字位置恢复）：
┌─────┐
│  A  │  ← 背景大，但文字位置正确 ✅
└─────┘
```

### 视觉效果

```
高亮前：
setting the action

高亮后（荧光笔效果）：
┌─────────────────────────┐
│ setting the action      │  ← 黄色背景包裹文字
└─────────────────────────┘
```

## 🎨 参数调整

### 当前参数

```css
padding: 2px 1px;      /* 上下2px，左右1px */
margin: -2px -1px;     /* 抵消 padding */
line-height: 1.3;      /* 行高1.3倍 */
```

### 如何调整

**想要更高的高亮**：
```css
padding: 3px 1px;      /* 增加上下 padding */
margin: -3px -1px;     /* 相应增加负 margin */
```

**想要更宽的高亮**：
```css
padding: 2px 2px;      /* 增加左右 padding */
margin: -2px -2px;     /* 相应增加负 margin */
```

**想要更紧凑的行高**：
```css
line-height: 1.2;      /* 减小行高 */
```

## 🔧 技术细节

### 为什么用负 margin？

```javascript
// 如果只用 padding
span.style.padding = '2px';
// 结果：背景变大了，但文字位置也移动了
// PDF 中的文字是绝对定位的，位置移动会导致错位

// 用负 margin 抵消
span.style.padding = '2px';
span.style.margin = '-2px';
// 结果：背景变大了，文字位置保持不变 ✅
```

### 为什么增加 line-height？

```css
/* line-height: 1 (默认) */
┌─────┐
│text │  ← 背景紧贴文字
└─────┘

/* line-height: 1.3 */
┌─────┐
│     │  ← 上方有空间
│text │
│     │  ← 下方有空间
└─────┘
```

### backgroundColor 的渲染范围

浏览器渲染 `backgroundColor` 时：
```
渲染范围 = content + padding
```

所以：
- `padding: 2px` → 背景扩展 2px
- `margin: -2px` → 元素位置不变
- 结果：背景大，位置对

## 📊 对比

### 之前（紧贴文字）

```css
padding: 0;
margin: 0;
line-height: 1;
```

```
┌────┐
│text│  ← 背景紧贴文字边缘
└────┘
```

### 现在（包裹文字）

```css
padding: 2px 1px;
margin: -2px -1px;
line-height: 1.3;
```

```
┌──────┐
│      │
│ text │  ← 背景包裹文字，像荧光笔
│      │
└──────┘
```

## 🎯 JavaScript 代码

JavaScript 代码不需要改变：

```javascript
// 直接设置背景色
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';

// CSS 会自动处理 padding 和 margin
// 背景会自动包裹文字
```

## 🧪 测试验证

### 测试步骤

1. 打开 PDF 文档
2. 选择一段文字
3. 右键选择黄色高亮
4. 观察效果：
   - ✅ 背景应该在文字下面
   - ✅ 背景应该包裹文字（上下左右有空间）
   - ✅ 文字位置应该准确（不偏移）

### 预期效果

```
选择前：
setting the action

选择后：
████████████████████████
█ setting the action   █  ← 黄色背景包裹
████████████████████████
```

## 💡 总结

### 核心技巧

> 用 `padding` 扩展背景，用负 `margin` 保持位置

### 实现代码

```css
/* CSS */
.pdf-text-layer span {
    padding: 2px 1px;
    margin: -2px -1px;
    line-height: 1.3;
    box-sizing: border-box;
}
```

```javascript
/* JavaScript */
span.style.backgroundColor = 'rgba(255, 255, 200, 0.6)';
```

### 效果

✅ 真实的荧光笔效果  
✅ 背景包裹文字  
✅ 文字位置准确  
✅ 代码简单

---

**日期**：2025年11月2日  
**版本**：4.0（荧光笔效果版）

