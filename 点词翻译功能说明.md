# 📖 点词翻译功能使用指南

## 🎯 功能概述

NPDF Reader现在支持**点词翻译**功能！点击工具栏上的"点词翻译"按钮后，就可以直接点击PDF中的任何单词，立即获得AI翻译结果。

---

## 🚀 如何使用

### 第一步：启用点词翻译模式

1. 打开PDF文件
2. 点击工具栏右侧的 **"点词翻译"按钮**（带有翻译图标）
3. 按钮变成**蓝色**表示已启用
4. 状态栏显示："点词翻译模式已开启 - 点击单词即可翻译"

### 第二步：点击单词获取翻译

1. 鼠标移动到任何单词上，单词会有**淡蓝色背景高亮**
2. **点击单词**
3. 立即弹出翻译窗口，显示：
   - 单词本身
   - 中文翻译
   - 音标
   - 例句

### 第三步：关闭翻译窗口

- 点击翻译窗口右上角的 **× 按钮**
- 或者再次点击"点词翻译"按钮关闭整个模式

---

## 💡 功能特性

### ✅ 已实现

1. **智能单词提取** - 自动去除标点符号，提取纯净单词
2. **即时翻译** - 点击即显示，响应迅速
3. **精美弹窗** - 美观的翻译结果展示
4. **智能定位** - 弹窗自动避免超出屏幕边界
5. **悬停效果** - 鼠标移到单词上有视觉反馈
6. **开关控制** - 可随时启用/关闭翻译模式

### 🎨 视觉设计

#### 按钮状态
- **未激活**：灰色图标
- **已激活**：蓝色背景 + 白色图标

#### 翻译弹窗
- **顶部**：单词显示 + 关闭按钮
- **中间**：翻译内容（翻译、音标、例句）
- **加载中**：显示旋转动画
- **错误**：红色背景错误提示

---

## 🔧 技术实现

### 架构设计

```
用户点击"点词翻译"按钮
    ↓
开启点词翻译模式（wordTranslateMode = true）
    ↓
为所有文本层添加word-translate-mode类
    ↓
绑定单词点击事件到每个span元素
    ↓
用户点击单词
    ↓
提取单词（去除标点）
    ↓
显示翻译弹窗（loading状态）
    ↓
调用翻译API
    ↓
显示翻译结果
```

### 核心方法

1. **toggleWordTranslateMode()** - 切换翻译模式
2. **bindWordClickEvents()** - 绑定点击事件
3. **handleWordClick()** - 处理单词点击
4. **extractWord()** - 提取纯净单词
5. **showWordTranslatePopup()** - 显示弹窗
6. **translateWord()** - 执行翻译
7. **callTranslationAPI()** - 调用翻译API

---

## 🌐 集成AI翻译API

当前实现包含**示例翻译数据**（introduction, book, the等单词）。要集成真实的AI翻译API，请修改 `callTranslationAPI()` 方法。

### 方案1：使用OpenAI GPT API

```javascript
async callTranslationAPI(word) {
    const response = await fetch('https://api.openai.com/v1/chat/completions', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': 'Bearer YOUR_API_KEY'
        },
        body: JSON.stringify({
            model: 'gpt-3.5-turbo',
            messages: [{
                role: 'user',
                content: `请翻译这个英文单词：${word}，并提供音标和例句`
            }]
        })
    });
    
    const data = await response.json();
    return data.choices[0].message.content;
}
```

### 方案2：使用Claude API

```javascript
async callTranslationAPI(word) {
    const response = await fetch('https://api.anthropic.com/v1/messages', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'x-api-key': 'YOUR_API_KEY',
            'anthropic-version': '2023-06-01'
        },
        body: JSON.stringify({
            model: 'claude-3-sonnet-20240229',
            max_tokens: 1024,
            messages: [{
                role: 'user',
                content: `请翻译这个英文单词：${word}，提供中文翻译、音标和例句`
            }]
        })
    });
    
    const data = await response.json();
    return data.content[0].text;
}
```

### 方案3：使用Google Translate API

```javascript
async callTranslationAPI(word) {
    const response = await fetch(
        `https://translation.googleapis.com/language/translate/v2?key=YOUR_API_KEY`,
        {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                q: word,
                source: 'en',
                target: 'zh-CN'
            })
        }
    );
    
    const data = await response.json();
    return `<p><strong>翻译：</strong>${data.data.translations[0].translatedText}</p>`;
}
```

### 方案4：使用本地AI模型

如果您有本地部署的AI模型：

```javascript
async callTranslationAPI(word) {
    const response = await fetch('http://localhost:8080/translate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ word: word, lang: 'zh' })
    });
    
    const data = await response.json();
    return `
        <p><strong>翻译：</strong>${data.translation}</p>
        <p><strong>音标：</strong>${data.phonetic || ''}</p>
    `;
}
```

---

## 📝 修改的文件

### 1. `src/reader.html`
- ✅ 添加"点词翻译"按钮到工具栏
- ✅ 添加翻译弹窗HTML结构

### 2. `src/styles/reader.css`
- ✅ 添加按钮激活状态样式
- ✅ 添加翻译弹窗样式
- ✅ 添加单词悬停效果
- ✅ 添加加载动画

### 3. `src/scripts/reader.js`
- ✅ 添加状态变量：`wordTranslateMode`, `wordTranslatePopup`
- ✅ 添加按钮事件监听
- ✅ 添加8个核心方法（约250行代码）

---

## 🎮 使用示例

### 示例1：翻译单个单词

1. 启用点词翻译模式
2. 点击 "introduction"
3. 看到弹窗显示：
   ```
   introduction
   
   翻译：介绍；引进；序言
   音标：/ˌɪntrəˈdʌkʃn/
   例句：
   • This is a brief introduction to the subject.
   • The introduction of new technology has changed our lives.
   ```

### 示例2：连续翻译多个单词

1. 保持翻译模式开启
2. 点击第一个单词 → 查看翻译
3. 关闭弹窗（点击×）
4. 点击第二个单词 → 查看翻译
5. 依此类推

---

## ⚙️ 配置选项

### 调整弹窗大小

在 `reader.css` 中修改：

```css
.word-translate-popup {
    min-width: 280px;  /* 最小宽度 */
    max-width: 400px;  /* 最大宽度 */
}
```

### 调整悬停高亮颜色

```css
.pdf-text-layer.word-translate-mode span:hover {
    background: rgba(74, 144, 226, 0.1);  /* 修改这里的颜色 */
}
```

### 调整翻译延迟

在 `reader.js` 的 `callTranslationAPI()` 方法中：

```javascript
// 模拟API调用延迟
await new Promise(resolve => setTimeout(resolve, 1000));  // 1秒，可修改
```

---

## 🐛 故障排除

### 问题1：点击单词没反应

**检查**：
1. 确认点词翻译模式已开启（按钮是蓝色）
2. 打开开发者工具Console查看是否有错误
3. 确认PDF已完全加载

**解决**：重新点击"点词翻译"按钮，或刷新页面

### 问题2：翻译一直显示"翻译中..."

**原因**：API调用失败或超时

**解决**：
1. 检查网络连接
2. 检查API密钥是否正确
3. 查看Console错误信息

### 问题3：弹窗位置不对

**原因**：屏幕边界计算问题

**解决**：调整 `showWordTranslatePopup()` 方法中的 `margin` 值

---

## 🎯 最佳实践

### 1. 使用场景

✅ **适合**：
- 阅读英文论文时查生词
- 学习新词汇
- 快速理解文章内容

❌ **不适合**：
- 翻译整段文字（应使用文本选择翻译）
- 翻译公式、代码（不是自然语言）

### 2. 提升体验

- 📌 阅读前先开启翻译模式
- 📌 遇到生词立即点击，加深印象
- 📌 查看例句理解单词用法
- 📌 定期关闭模式，避免误触

### 3. 性能优化

- 翻译结果可以考虑**本地缓存**
- 对常用词汇建立**离线词库**
- 使用**批量翻译**减少API调用

---

## 📊 数据统计（可扩展）

未来可以添加的统计功能：

1. **翻译历史记录**
   - 记录查询过的所有单词
   - 按时间排序
   - 导出为词汇表

2. **学习进度**
   - 统计查询次数
   - 标记已掌握的单词
   - 生成复习计划

3. **词频分析**
   - 分析文档中的高频词汇
   - 突出显示生词
   - 智能推荐学习顺序

---

## 🎓 技术要点总结

### 1. 单词提取算法

使用正则表达式去除标点符号：
```javascript
text.replace(/[^\w\s-]/g, '').trim()
```

### 2. 弹窗定位算法

```javascript
// 边界检查
if (left + popupWidth > window.innerWidth) {
    left = x - popupWidth - margin;  // 向左显示
}
if (top + popupHeight > window.innerHeight) {
    top = y - popupHeight - margin;  // 向上显示
}
```

### 3. 事件绑定优化

```javascript
span.addEventListener('click', this.handleWordClick.bind(this));
```
使用 `.bind(this)` 确保方法中的 `this` 指向正确

---

## 🚀 下一步计划

基于点词翻译功能，可以进一步开发：

1. **生词本功能** - 自动收集查询的单词
2. **离线词典** - 集成本地词典数据库
3. **单词发音** - 集成TTS朗读功能
4. **助记笔记** - 为单词添加个人笔记
5. **复习提醒** - 艾宾浩斯遗忘曲线提醒

---

**功能完成日期**：2025年10月16日  
**版本**：NPDF Reader v1.1.0  
**状态**：✅ 已实现并可用（需集成真实AI API）

---

> **"让阅读英文文献像母语一样轻松！"**  
> — NPDF Reader 点词翻译功能

