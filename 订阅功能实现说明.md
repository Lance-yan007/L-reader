# 订阅功能实现说明

## ✅ 已完成的工作

### 1. UI界面实现

#### 个人中心订阅管理卡片
- ✅ 订阅状态显示（免费版/月订阅/年订阅）
- ✅ 订阅到期时间显示
- ✅ 使用量统计显示（点词翻译、AI助手、生词本）
- ✅ 使用量进度条（带颜色警告）
- ✅ 升级订阅按钮
- ✅ 管理订阅按钮（付费用户可见）

#### 订阅升级弹窗
- ✅ 月订阅卡片（¥39.9/月）
- ✅ 年订阅卡片（¥299/年，推荐）
- ✅ 功能对比展示
- ✅ 价格和优惠信息

### 2. 功能逻辑实现

#### 订阅状态管理
- ✅ 从Supabase加载订阅状态
- ✅ 检查订阅是否过期
- ✅ 自动更新订阅状态
- ✅ UI动态更新

#### 使用量统计
- ✅ 从Supabase加载今日使用量
- ✅ 统计生词本总数
- ✅ 实时显示使用量
- ✅ 进度条可视化

#### 订阅限制配置
```javascript
免费版：
- 点词翻译：每日50次
- AI助手：每日10次
- 生词本：最多100个单词

月订阅：
- 点词翻译：每日200次
- AI助手：每日50次
- 生词本：最多500个单词

年订阅：
- 点词翻译：无限
- AI助手：无限
- 生词本：无限
```

### 3. 数据库设计

#### 表结构
- ✅ `subscriptions` 表：存储订阅信息
- ✅ `usage_stats` 表：存储每日使用量统计
- ✅ 索引优化
- ✅ 行级安全策略（RLS）
- ✅ 自动更新触发器

### 4. 文件清单

#### 新增/修改的文件
1. **src/profile.html** - 添加订阅管理UI
2. **src/styles/profile.css** - 添加订阅相关样式
3. **src/scripts/profile.js** - 实现订阅功能逻辑
4. **src/utils/subscription.js** - 订阅工具函数（Node.js环境）
5. **create-subscription-tables.sql** - 数据库表创建脚本

---

## 📋 使用说明

### 1. 数据库设置

在 Supabase SQL Editor 中执行 `create-subscription-tables.sql` 脚本：

```sql
-- 执行脚本创建表
-- 1. subscriptions 表
-- 2. usage_stats 表
-- 3. RLS策略
-- 4. 触发器
```

### 2. 功能使用

#### 查看订阅状态
1. 打开个人中心页面
2. 在"订阅管理"卡片中查看：
   - 当前订阅类型（免费版/月订阅/年订阅）
   - 订阅到期时间
   - 今日使用量统计

#### 升级订阅
1. 点击"升级订阅"按钮
2. 在弹窗中选择订阅计划：
   - 月订阅：¥39.9/月
   - 年订阅：¥299/年（推荐）
3. 点击"选择XX订阅"按钮
4. 完成支付（需要集成App Store内购）

#### 管理订阅
1. 付费用户可以看到"管理订阅"按钮
2. 点击后可以：
   - 查看订阅详情
   - 取消订阅
   - 修改订阅计划

---

## 🔧 待完成的工作

### 1. App Store内购集成 ⏳

**当前状态**：UI和逻辑已实现，但内购API未集成

**需要完成**：
- [ ] 集成 StoreKit 2（macOS）
- [ ] 创建订阅产品（月订阅、年订阅）
- [ ] 实现购买流程
- [ ] 验证收据
- [ ] 同步订阅状态到Supabase

**参考代码位置**：
- `src/scripts/profile.js` 的 `handleSelectPlan()` 方法
- 需要替换模拟代码为真实的StoreKit API调用

### 2. 使用量追踪集成 ⏳

**当前状态**：数据库表已创建，但功能使用处未集成

**需要完成**：
- [ ] 在点词翻译功能中集成使用量追踪
- [ ] 在AI助手功能中集成使用量追踪
- [ ] 在生词本功能中集成使用量追踪
- [ ] 实现限制检查逻辑

**参考代码位置**：
- `src/scripts/reader.js` - 点词翻译和AI助手
- `src/scripts/vocabulary.js` - 生词本
- `src/utils/subscription.js` - 工具函数

### 3. 订阅到期提醒 ⏳

**需要完成**：
- [ ] 订阅到期前7天提醒
- [ ] 订阅到期后宽限期处理
- [ ] 自动降级为免费版

---

## 🎯 订阅限制检查示例

### 在功能中使用限制检查

```javascript
// 在 reader.js 中
const { checkFeatureLimit, updateUsage } = require('../utils/subscription');

// 检查点词翻译限制
async function translateWord(word) {
    // 1. 获取用户订阅状态
    const subscription = await getSubscriptionStatus(supabase, userId);
    
    // 2. 获取今日使用量
    const usage = await getTodayUsage(supabase, userId);
    
    // 3. 检查限制
    const limitCheck = checkFeatureLimit(
        subscription.planType,
        'wordTranslations',
        usage
    );
    
    if (!limitCheck.allowed) {
        // 显示升级提示
        showUpgradePrompt();
        return;
    }
    
    // 4. 执行翻译
    const result = await callTranslationAPI(word);
    
    // 5. 更新使用量
    await updateUsage(supabase, userId, 'wordTranslations', 1);
    
    return result;
}
```

---

## 📊 数据库表结构

### subscriptions 表
```sql
- id: UUID (主键)
- user_id: UUID (外键 -> auth.users)
- plan_type: VARCHAR(20) ('free', 'monthly', 'yearly')
- status: VARCHAR(20) ('active', 'expired', 'cancelled')
- start_date: TIMESTAMP
- end_date: TIMESTAMP
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

### usage_stats 表
```sql
- id: UUID (主键)
- user_id: UUID (外键 -> auth.users)
- date: DATE
- word_translations: INT (点词翻译次数)
- ai_chat_count: INT (AI助手次数)
- vocabulary_count: INT (生词本数量)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

---

## 🎨 UI设计说明

### 订阅状态徽章
- **免费版**：灰色背景
- **月订阅**：蓝色背景
- **年订阅**：蓝色渐变背景

### 使用量进度条
- **正常**：蓝色（< 70%）
- **警告**：橙色（70-90%）
- **危险**：红色（> 90%）
- **无限**：不显示进度条

### 订阅升级弹窗
- 响应式设计（移动端单列，桌面端双列）
- 年订阅卡片突出显示（推荐标签）
- 清晰的功能对比
- 优惠信息展示

---

## ⚠️ 注意事项

1. **数据库表创建**：必须先执行SQL脚本创建表
2. **RLS策略**：确保行级安全策略正确配置
3. **内购集成**：需要Apple Developer账号和App Store Connect配置
4. **使用量追踪**：需要在各个功能中集成追踪逻辑
5. **订阅验证**：需要验证App Store收据，防止伪造

---

## 🚀 下一步计划

1. **集成App Store内购**（优先级：高）
2. **在功能中集成使用量追踪**（优先级：高）
3. **实现限制检查逻辑**（优先级：高）
4. **订阅到期提醒**（优先级：中）
5. **订阅管理功能完善**（优先级：中）

---

## 📝 测试建议

1. **订阅状态显示**：测试不同订阅状态的UI显示
2. **使用量统计**：测试使用量的实时更新
3. **限制检查**：测试达到限制时的提示
4. **升级流程**：测试订阅升级的完整流程
5. **数据库操作**：测试订阅创建、更新、查询

---

## 💡 提示

- 订阅功能的核心逻辑已实现
- UI界面已完整设计
- 数据库表结构已创建
- 下一步主要是集成App Store内购和使用量追踪

如有问题，请参考代码注释或联系开发团队。

