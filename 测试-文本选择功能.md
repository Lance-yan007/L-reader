# 🧪 PDF文本选择功能测试指南

## 测试时间
2025年10月16日

## 测试目标
验证PDF文本选择功能是否正常工作，确保用户可以像在网页上一样选择PDF中的文字。

## 测试环境
- **应用**: NPDF Reader v1.0.0
- **平台**: macOS
- **测试文件**: test-files/real-text.pdf, test-files/sample.pdf

## 测试步骤

### 第一步：启动应用
```bash
cd /Users/yugongtian/NPDF
npm start
```

### 第二步：打开PDF文件
1. 点击主界面的"打开文件"按钮
2. 选择 `test-files/real-text.pdf`
3. 等待PDF加载完成

### 第三步：检查文本层是否加载
打开开发者工具（View -> Toggle Developer Tools），在Console中查看日志：

**预期日志**：
```
开始提取页面1的文本内容...
页面1文本提取完成，共X个文本项
页面1文本层渲染完成
```

### 第四步：测试文本选择
1. **鼠标悬停测试**
   - 将鼠标移动到PDF文档上
   - ✅ 预期：光标变成文本光标（I-beam）

2. **单击选择测试**
   - 单击文档中的某个位置
   - 拖动鼠标选择一段文字
   - ✅ 预期：
     - 文字被选中，显示蓝色半透明高亮
     - Console中显示：`选中的文本: [您选中的内容]`

3. **复制测试**
   - 选中一段文字
   - 按 `Cmd+C` (Mac) 或 `Ctrl+C` (Windows)
   - 打开文本编辑器，按 `Cmd+V` 粘贴
   - ✅ 预期：成功复制选中的文字

4. **跨行选择测试**
   - 从一行的开头拖动到另一行的结尾
   - ✅ 预期：可以选择多行文字

5. **双击选择测试**
   - 双击某个单词
   - ✅ 预期：整个单词被选中

6. **全选测试**
   - 按 `Cmd+A` (Mac) 或 `Ctrl+A` (Windows)
   - ✅ 预期：整个页面的文字被选中

### 第五步：测试多页PDF
1. 打开多页PDF文件
2. 分别在不同页面上选择文字
3. ✅ 预期：每一页的文字都可以正常选择

### 第六步：测试缩放后的选择
1. 使用缩放按钮放大PDF
2. 尝试选择文字
3. ✅ 预期：缩放后仍然可以正常选择文字，位置准确

### 第七步：检查DOM结构
在开发者工具的Elements标签中检查：

```javascript
// 检查文本层数量
document.querySelectorAll('.pdf-text-layer').length
// 应该等于PDF页数

// 检查第一个文本层的文本项数量
document.querySelector('.pdf-text-layer').children.length
// 应该大于0

// 查看文本层内容示例
document.querySelector('.pdf-text-layer span').textContent
// 应该显示PDF中的某个文字
```

## 测试检查清单

### 核心功能 ✅
- [ ] PDF文件成功加载
- [ ] 文本层成功创建和渲染
- [ ] 鼠标光标正确显示为文本光标
- [ ] 可以用鼠标选择文字
- [ ] 选中的文字显示高亮
- [ ] 可以复制选中的文字
- [ ] Console中正确显示选中的文本

### 交互体验 ✅
- [ ] 单击选择流畅
- [ ] 拖动选择流畅
- [ ] 双击选择单词工作正常
- [ ] 三击选择段落（如果实现）
- [ ] 全选功能正常

### 边界情况 ✅
- [ ] 多页PDF每页都可选择
- [ ] 缩放后文本位置准确
- [ ] 旋转后文本位置准确（如果实现）
- [ ] 空白区域点击不报错
- [ ] 纯图片PDF不报错（无文本层）

### 性能 ✅
- [ ] 文本层渲染时间 < 5秒/页
- [ ] 选择响应延迟 < 100ms
- [ ] 内存使用合理
- [ ] 无明显卡顿

## 调试技巧

### 查看文本层是否渲染
```javascript
// 显示文本层边界（调试模式）
document.querySelectorAll('.pdf-text-layer').forEach(layer => {
    layer.classList.add('debug');
    layer.style.opacity = '0.5'; // 提高透明度便于查看
});
```

### 检查文本项位置
```javascript
// 查看所有文本项的位置
document.querySelectorAll('.pdf-text-layer span').forEach((span, i) => {
    if (i < 10) { // 只看前10个
        console.log(`文本 ${i}:`, {
            content: span.textContent,
            left: span.style.left,
            top: span.style.top,
            fontSize: span.style.fontSize
        });
    }
});
```

### 监听选择事件
```javascript
document.addEventListener('text-selected', (e) => {
    console.log('=== 文本选择事件 ===');
    console.log('选中文本:', e.detail.text);
    console.log('文本长度:', e.detail.text.length);
    
    // 可以在这里添加自动翻译等功能
});
```

## 问题记录

### 问题1：文本无法选择
- **现象**：鼠标点击文档没有反应
- **可能原因**：
  1. 文本层没有渲染
  2. CSS pointer-events设置错误
  3. 文本层被其他元素遮挡
- **解决方法**：检查Console日志，查看DOM结构

### 问题2：文本位置不准确
- **现象**：选中的文字和显示的文字不对应
- **可能原因**：
  1. 坐标转换计算错误
  2. 字体大小计算不准确
  3. PDF使用特殊编码
- **解决方法**：开启调试模式查看文本边界

### 问题3：缩放后文本错位
- **现象**：放大缩小后文本层和Canvas不对齐
- **可能原因**：文本层缩放逻辑与Canvas不同步
- **解决方法**：检查applyZoom方法

## 测试结果

### 测试日期：_____________

### 测试结果：
- [ ] ✅ 通过
- [ ] ⚠️ 部分通过（记录问题）
- [ ] ❌ 未通过（详细说明）

### 测试备注：
```
[在这里记录测试过程中发现的问题、建议和改进点]
```

### 截图：
[如有必要，添加测试截图]

---

## 下一步

测试通过后，可以继续开发以下功能：
1. **右键菜单**：选中文字后右键显示"翻译"、"添加注释"等选项
2. **快捷翻译**：选中文字后显示浮动翻译按钮
3. **高亮保存**：将选中的文字高亮并保存到数据库
4. **词汇表**：自动提取选中的单词并添加到词汇表

