# 账号系统配置说明

## ✅ 已完成的工作

1. ✅ 安装 Supabase 客户端
2. ✅ 创建登录/注册页面
3. ✅ 创建认证管理模块
4. ✅ 修改主界面，添加用户信息显示
5. ✅ 添加登出功能

## 📋 需要配置的内容

### 步骤1：获取 Supabase 密钥

1. 登录 Supabase：https://app.supabase.com
2. 选择你的项目
3. 点击左侧菜单 "Settings" → "API"
4. 记录以下信息：
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon/public key**: `eyJhbGc...`（很长的字符串）

### 步骤2：配置 Supabase 客户端

打开文件：`src/utils/supabase.js`

找到这两行：
```javascript
const supabaseUrl = process.env.SUPABASE_URL || 'YOUR_SUPABASE_URL';
const supabaseAnonKey = process.env.SUPABASE_ANON_KEY || 'YOUR_SUPABASE_ANON_KEY';
```

替换为你的实际值：
```javascript
const supabaseUrl = 'https://你的项目ID.supabase.co';
const supabaseAnonKey = '你的anon key';
```

### 步骤3：测试

1. 启动应用：
   ```bash
   npm start
   ```

2. 应该会看到登录页面
3. 点击"注册"，创建新账号
4. 注册成功后，会自动跳转到主界面
5. 在侧边栏底部可以看到用户信息

## 🎯 功能说明

### 登录/注册
- 支持邮箱注册
- 支持用户名（可选）
- 密码长度至少6位
- 注册成功后自动登录

### 用户信息显示
- 侧边栏底部显示用户名和邮箱
- 点击退出按钮可以登出

### 自动跳转
- 未登录时自动跳转到登录页面
- 登录后自动跳转到主界面
- 登出后自动跳转到登录页面

## ⚠️ 注意事项

1. **Supabase 配置**：必须配置正确的 URL 和 Key，否则无法使用
2. **数据库表**：确保已经创建了所有需要的表（users、documents、translations、annotations）
3. **RLS 策略**：确保已经设置了行级安全策略

## 🔧 故障排除

### 问题1：登录页面显示错误
- 检查 Supabase URL 和 Key 是否正确
- 检查网络连接
- 查看浏览器控制台的错误信息

### 问题2：注册失败
- 检查邮箱格式是否正确
- 检查密码长度是否至少6位
- 检查 Supabase 项目是否正常运行

### 问题3：无法跳转到主界面
- 检查是否成功登录
- 检查 main.html 文件是否存在
- 查看浏览器控制台的错误信息

## 📝 下一步

完成配置后，可以继续开发：
1. 数据同步功能（翻译记录、标注数据）
2. 个人中心页面
3. 多设备同步

